import React from 'react';
import './filter-category.scss';

const FilterCategory = () => {
    return (
        <div>
            FilterCategory
        </div>
    );
};

export default FilterCategory;